/*
  Line 17 is the problem as im not sure what to put in for the slides class arguments(c (for color) and p (prizeAmount)
  I create a new class object[?] for slides here because i need to access the getColor and getPrizeAmount Methods to print the 
  total cash amount and double it if all slice colors are the same
*/


import java.util.ArrayList;

// Main class for Game wheel and Slice 
public class Main {
	

	public static void main(String[] args) {
		//Allows us to use methods from other classes
		GameWheel getGame = new GameWheel();
		Slice getSlice = new Slice(getGame.getSlices());

		// adds up the sum of all earned money
		int totalMoney = 0;
		
		// we will check if all values of the array hold the same string value 
		ArrayList<String> allColors = new ArrayList<String>();
		
		
		// Spins 3 times and each time 
		for(int i = 0; i < 3; i ++) 
		{
			getGame.spinWheel();
			System.out.println("Spin " + i + " = " + getSlice.toString());
			totalMoney += getSlice.getPrizeAmount();
			allColors.add(getSlice.getColor());
			
		}
		
		// check if all colors match 
		if((allColors.get(0) == allColors.get(1)) && (allColors.get(0) == allColors.get(2)))
		{
			// double money if all values are the same Color and PRINT
			System.out.println("Total Money = " + totalMoney*2);
			System.out.println("All Slides are the same Color EARN DOUBLE!!");
		}
		else 
		{
			// print regular money with no additions 
			System.out.println("Total Money = " + totalMoney);
		}
		
		/*  Expected Output 1
		 *  Total prize money: $7000
		 * 
			Spin 1 - Color: black, Prize Amount: $4000
			Spin 2 - Color: blue, Prize Amount: $2400
			Spin 3 - Color: blue, Prize Amount: $600
			
			
			Expected Output 2 
			Total prize money: $820

			Spin 1 - Color: red, Prize Amount: $50
			Spin 2 - Color: red, Prize Amount: $130
			Spin 3 - Color: red, Prize Amount: $230
			"All Slides are the same Color EARN DOUBLE!!"
		 */
		
		
	}

}
